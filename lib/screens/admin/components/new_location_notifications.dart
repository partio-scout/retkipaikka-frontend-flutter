import 'package:flutter/material.dart';

class NewLocationNotifications extends StatelessWidget {
  const NewLocationNotifications({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
