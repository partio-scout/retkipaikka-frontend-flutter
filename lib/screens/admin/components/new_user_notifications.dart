import 'package:flutter/material.dart';

class NewUserNotifications extends StatelessWidget {
  const NewUserNotifications({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
